<?php
return array(
	/*
	 * * Дочерняя
	 * * Таблица для связки одного объекта к другому MTM
	 * таким образом возможна (ассоциации объектов)
	 *
	 * +id = первичный ключ для таблицы
	 * +from_self_id = id объекта
	 * +to_self_id = id объекта на который ссылается объект from_self_id
	 */

);