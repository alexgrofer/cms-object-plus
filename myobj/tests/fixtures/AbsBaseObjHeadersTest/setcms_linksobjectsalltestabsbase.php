<?php
return array(
	/*
	 * * Ссылка
	 * * Таблица ссылок объектов, для каждого объекта который имеет возможность
	 * ссылаться на другой нужна запись в этой таблице
	 *
	 * +id = первичный ключ для таблицы
	 * +idobj = id объекта
	 * +uclass_id = id класса объекта
	 */

);